const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2); // (raio, 2) = raio elevado ao quadrado
// Math é um objeto que tem um constante (PI) e uma função que calcula a potência (pow(power))

console.log(area);
console.log(typeof Math);